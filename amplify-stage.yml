version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm install
    build:
      commands:
        - npm run build:stage
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
